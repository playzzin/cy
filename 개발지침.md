# 스마트 건설 현장 관리 시스템 개발 지침

최종 기준일: 2026-07-06

이 문서는 현재 저장소의 실제 구조와 운영 방식에 맞춘 개발 기준이다. 예전 초기 구축용 샘플 코드보다 현재 소스, 보안 규칙, CI 설정을 우선한다.

## 1. 기준 원칙

- 새 코드는 기본적으로 TypeScript로 작성한다. `src`에는 `.ts`와 `.tsx`를 우선 사용하고, 기존 JS 파일을 무리하게 확장하지 않는다.
- Firestore 컬렉션명은 현재 운영 컬렉션명을 따른다. 특히 작업일보는 `daily_reports`가 정식 컬렉션이며 `dailyReports`를 새 코드에 사용하지 않는다.
- 공통 데이터 접근은 기존 `services`, `repositories`, `constants`, `security` 계층을 먼저 확인한 뒤 확장한다.
- 보안 규칙과 프론트엔드 권한 정책을 함께 맞춘다. 화면에서 숨겼더라도 Firestore Rules가 허용하지 않으면 동작하지 않아야 한다.
- 변경 후에는 최소 `npm run typecheck`를 실행한다. 배포 또는 PR 전에는 `npm run verify:project`와 `npm run build`를 통과시킨다.
- 대규모 리팩터링보다 현재 모듈 경계에 맞춘 작은 변경을 우선한다.

## 2. 기술 스택

### 프론트엔드

- React 18
- TypeScript strict mode
- Create React App 기반 `react-scripts` 래퍼
- Tailwind CSS
- React Router
- Zustand
- Zod
- Radix UI
- lucide-react
- FontAwesome
- Recharts
- AG Grid
- Refine 일부 관리 화면

### Firebase

- Firebase Auth
- Cloud Firestore
- Firebase Storage
- Cloud Functions for Firebase
- Firebase Hosting
- Firestore Rules
- Storage Rules

### 문서/엑셀/데이터 처리

- `xlsx`
- `xlsx-js-style`
- `exceljs`
- `file-saver`
- `date-fns`
- `date-fns-tz`

### 서버/Functions

- Functions 런타임: Node 22
- Functions 소스: `functions/src`
- Functions 빌드: `npm --prefix functions run build`

## 3. 주요 디렉터리

```text
smart-construction/
├── src/
│   ├── app/                 # 앱 초기화 및 상위 조립 코드
│   ├── assets/              # 정적 자산과 사용자 가이드
│   ├── components/          # 재사용 UI 컴포넌트
│   ├── config/              # Firebase 등 런타임 설정
│   ├── constants/           # 컬렉션/메뉴/상수 정의
│   ├── contexts/            # React Context 기반 상태
│   ├── features/            # 기능별 순수 로직, 훅, 테스트
│   ├── hooks/               # 공통 React hooks
│   ├── pages/               # 라우트 단위 화면
│   ├── providers/           # 외부/프레임워크 provider
│   ├── repositories/        # Firestore repository 계층
│   ├── routes/              # 라우팅 보조 코드
│   ├── security/            # 권한/접근 정책
│   ├── services/            # 업무 서비스와 Firebase 연동
│   ├── types/               # 공통 타입, Zod 스키마
│   └── utils/               # 공통 유틸리티
├── functions/               # Firebase Functions
├── scripts/                 # 검증, 마이그레이션, 운영 보조 스크립트
├── docs/                    # 설계 문서
├── firestore.rules          # Firestore 보안 규칙
├── firestore.indexes.json   # Firestore 인덱스
├── storage.rules            # Storage 보안 규칙
├── firebase.json            # Firebase Hosting/Functions 설정
├── package.json             # 앱 스크립트와 의존성
└── tailwind.config.js       # Tailwind 설정
```

## 4. 실행 및 검증 명령

### 로컬 실행

```bash
npm install
npm start
```

`npm start`는 `scripts/react-scripts-start.js`를 통해 실행된다.

### 기본 검증

```bash
npm run typecheck
npm run test:ci
npm run functions:build
```

### 프로젝트 전체 검증

```bash
npm run verify:project
```

이 명령은 다음을 순서대로 확인한다.

- 주요 JSON 문법
- Java 설치 여부 경고
- TypeScript typecheck
- React test suite
- Firebase Functions build

### 빌드와 Hosting 검증

```bash
npm run build
npm run verify:hosting-build
```

### 보안 감사

```bash
npm run audit:all
```

## 5. CI와 배포 기준

GitHub Actions는 `.github/workflows/quality.yml`을 따른다.

필수 단계:

- Node 22 사용
- 루트 의존성 설치: `npm ci`
- Functions 의존성 설치: `npm --prefix functions ci`
- 프로젝트 검증: `npm run verify:project`
- Hosting 앱 빌드: `npm run build`
- Hosting 빌드 검증: `npm run verify:hosting-build`

Firebase Hosting 배포 전에는 `firebase.json`의 `hosting.predeploy`가 다음을 실행한다.

```bash
npm run build
npm run verify:hosting-build
```

Functions 배포 전에는 다음이 실행된다.

```bash
npm --prefix functions run build
```

## 6. Firebase 설정

Firebase 앱 초기화 기준 파일:

- `src/config/firebaseBase.ts`
- `src/config/firebase.ts`
- `src/config/firebaseAuth.ts`

필수 환경 변수:

```text
REACT_APP_FIREBASE_API_KEY=
REACT_APP_FIREBASE_AUTH_DOMAIN=
REACT_APP_FIREBASE_PROJECT_ID=
REACT_APP_FIREBASE_STORAGE_BUCKET=
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=
REACT_APP_FIREBASE_APP_ID=
```

선택 환경 변수:

```text
REACT_APP_FIREBASE_MEASUREMENT_ID=
REACT_APP_USE_EMULATORS=true
```

주의사항:

- 로컬 값은 `.env.local`에 둔다.
- 예시는 `.env.local.example`을 갱신한다.
- 실제 비밀값은 커밋하지 않는다.
- Functions 환경 변수는 `functions/.env` 또는 Firebase 환경 설정을 사용한다.
- Kakao 관련 Functions 호출은 `asia-northeast3` 리전을 기준으로 한다.
- `REACT_APP_USE_EMULATORS=true`일 때 Functions emulator는 `localhost:5001`을 사용한다.

## 7. Firestore 컬렉션 기준

컬렉션 기준은 `src/constants/collectionConfig.ts`, `src/security/erpAccessPolicy.ts`, `firestore.rules`를 함께 확인한다.

### 핵심 컬렉션

```text
companies
teams
workers
office_staff
app_users
login_logs
sites
daily_dispatches
field_schedule_requests
schedule_confirmation_boards
daily_reports
daily_report_workers
daily_report_logs
database_logs
audit_logs
system_logs
menus
menu_configs
positions
settings
system_configs
```

### 정산/소개소/지원 주요 컬렉션

```text
recruiting_referrers
service_worker_referrals
service_referral_daily_lines
service_referral_monthly_settlements
service_referral_settings
service_worker_history
service_referral_payments
service_referral_deposits
service_referral_receivables
advance_payments
payments
tax_invoices
receivables
vehicles
vehicle_assignments
vehicle_expenses
vehicle_billing_documents
card_billing_logs
accommodations
accommodation_billing_documents
accommodation_billing_line_items
team_expense_claims
material_logs
workbook_ledger_logs
```

### 금지 또는 주의할 이름

- `dailyReports`: 신규 코드에서 사용 금지. 기존 백업/호환 코드 외에는 `daily_reports`를 사용한다.
- `laborCosts`: 현재 운영 모델과 맞지 않는 초기 샘플명이다. 급여/정산 관련 실제 컬렉션과 서비스를 확인한다.
- 임의 복수형 컬렉션명: 새 컬렉션은 `collectionConfig`, Rules, 접근 정책, 서비스, 마이그레이션 문서까지 함께 반영한다.

## 8. 권한과 보안

Firestore Rules는 단순 로그인 허용 방식이 아니다. 역할 그룹과 컬렉션 그룹별 정책을 사용한다.

주요 역할 그룹:

```text
admin
payroll
finance
office
site
support
audit
user
```

개발 기준:

- 새 컬렉션을 추가하면 `firestore.rules`의 컬렉션 분류 함수에 포함할지 결정한다.
- 프론트엔드 권한 판단은 `src/security`의 정책을 우선 확인한다.
- 감사/로그 컬렉션은 생성은 허용하되 수정/삭제를 제한하는 패턴을 유지한다.
- 관리자 전용 설정 컬렉션은 일반 사용자 쓰기를 허용하지 않는다.
- 사용자 본인 생성/수정 허용은 필드 whitelist 방식으로 제한한다.
- Rules 변경 시 관련 테스트가 있으면 함께 갱신한다.

## 9. 코드 작성 규칙

### TypeScript

- `strict` 모드를 전제로 작성한다.
- `any`는 외부 입력, 마이그레이션, 레거시 호환처럼 필요한 경우에만 좁은 범위에서 사용한다.
- 공통 타입은 `src/types` 또는 해당 feature 내부에 둔다.
- Firestore 문서 입력값은 가능하면 Zod 스키마 또는 명시적 normalizer를 거친다.

### React

- 라우트 단위 화면은 `src/pages`에 둔다.
- 기능별 순수 계산, 정책, 상태 파생 로직은 `src/features`에 둔다.
- 재사용 가능한 UI는 `src/components`에 둔다.
- 화면 안에 Firestore 쿼리와 복잡한 계산을 길게 넣지 말고 service/repository/feature로 분리한다.
- 내부 운영 도구 성격이 강하므로 정보 밀도, 스캔 가능성, 반복 작업 효율을 우선한다.

### Services와 Repositories

- Firestore 직접 접근은 기존 service/repository 패턴을 따른다.
- 컬렉션명을 문자열로 반복하지 말고 기존 상수, 서비스, repository를 먼저 확인한다.
- 저장/수정/삭제가 업무 로그를 요구하는 영역이면 log service도 함께 갱신한다.
- 대량 작업은 배치 크기, 실패 복구, 진행 로그를 고려한다.

### 날짜와 금액

- 날짜 키는 특별한 이유가 없으면 `yyyy-MM-dd` 형식을 사용한다.
- 월 단위 키는 `yyyy-MM` 형식을 사용한다.
- 타임존이 중요한 업무일자는 `date-fns-tz` 사용을 검토한다.
- 금액은 화면 표시 전까지 숫자 타입을 유지한다.

### UI

- Tailwind 설정은 `tailwind.config.js`를 기준으로 한다.
- 신규 아이콘은 가능한 경우 `lucide-react`를 우선 사용한다.
- 표/대량 데이터 화면은 기존 AG Grid 패턴을 검토한다.
- 차트는 현재 프로젝트에서 많이 쓰는 Recharts 패턴을 우선 확인한다.
- 모달/드롭다운은 기존 Radix 기반 컴포넌트나 프로젝트 공통 컴포넌트를 먼저 확인한다.
- 텍스트가 버튼, 카드, 테이블 셀에서 넘치지 않도록 모바일/데스크톱을 함께 고려한다.

## 10. 테스트 기준

변경 범위별 권장 검증:

- 타입/서비스/권한 로직 변경: `npm run typecheck`
- 순수 로직 변경: 관련 `*.test.ts` 또는 `*.test.tsx` 추가/수정
- Firestore Rules 또는 보안 정책 변경: `src/security` 테스트와 Rules 영향 범위 확인
- Functions 변경: `npm run functions:build`
- UI/라우팅 변경: `npm run build`
- 배포 전: `npm run verify:project && npm run build && npm run verify:hosting-build`

## 11. 데이터 마이그레이션 기준

마이그레이션은 운영 데이터에 직접 영향을 준다. 다음을 지킨다.

- 실행 전 백업을 만든다.
- dry-run 또는 읽기 전용 점검 스크립트를 먼저 작성한다.
- 컬렉션명 변경은 호환 레이어와 정리 계획을 함께 둔다.
- 대량 삭제 스크립트는 대상 컬렉션과 조건을 코드에서 명확히 제한한다.
- 실행 로그와 결과 요약을 남긴다.
- `dailyReports` 같은 과거 컬렉션명은 새 데이터 저장 대상이 아니다.

관련 문서:

- `COLLECTION_MIGRATION_GUIDE.md`
- `scripts/migrate_from_json.js`
- `scripts/backup_firestore.ts`
- `scripts/restore-firestore.js`
- `scripts/scan_firestore_usage.ts`

## 12. 로그와 로컬 산출물

`.gitignore`는 다음을 제외한다.

- `node_modules`
- `build`, `dist`, `coverage`
- `.env*`
- 로그 파일
- 로컬 브라우저/자동화 프로필
- 루트 스크린샷과 진단 JSON
- 백업/대용량/비밀 파일

새로운 로컬 산출물을 만들면 커밋 대상인지 먼저 판단한다. 재현 가능한 로그, 캡처, 임시 JSON은 기본적으로 커밋하지 않는다.

## 13. 문서 갱신 기준

다음 변경은 문서도 함께 갱신한다.

- 새 Firestore 컬렉션 추가
- 컬렉션명 변경 또는 마이그레이션
- 권한 역할 또는 Rules 변경
- 배포/검증 명령 변경
- 환경 변수 추가
- 주요 기능 모듈 추가
- 외부 API 또는 Functions 리전 변경

기능별 세부 설계는 `docs/`에 별도 문서로 두고, 이 파일에는 프로젝트 공통 원칙과 기준만 유지한다.

## 14. 빠른 체크리스트

작업 시작 전:

- 관련 `pages`, `features`, `services`, `types`, `security` 파일을 먼저 찾는다.
- 같은 기능의 기존 패턴을 확인한다.
- 컬렉션명과 권한 정책을 확인한다.

작업 중:

- 새 코드에는 TypeScript 타입을 둔다.
- Firestore 접근은 service/repository 경계를 지킨다.
- 사용자 입력은 validation/normalization을 거친다.
- 로그가 필요한 저장 작업인지 확인한다.

작업 완료 전:

- `dailyReports` 등 레거시 컬렉션명을 새로 추가하지 않았는지 확인한다.
- `npm run typecheck`를 실행한다.
- 영향이 큰 변경은 `npm run verify:project`와 `npm run build`까지 실행한다.
- 환경 변수, 컬렉션, 권한이 바뀌었다면 문서를 갱신한다.
