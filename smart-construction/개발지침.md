# 🚀 스마트 건설 현장 관리 시스템 개발 지침

## 📋 기술 스택
- **Firebase**: Auth, Firestore, Hosting (Serverless)
- **SheetJS (xlsx)**: Excel 파일 내보내기
- **Chart.js**: 데이터 시각화
- **Tailwind CSS**: 스타일링
- **React**: 프론트엔드 프레임워크

## 🗂️ 프로젝트 구조
```
smart-construction/
├── src/
│   ├── components/
│   │   ├── auth/
│   │   │   ├── Login.jsx
│   │   │   └── Register.jsx
│   │   ├── dashboard/
│   │   │   ├── Dashboard.jsx
│   │   │   └── DashboardCard.jsx
│   │   ├── reports/
│   │   │   ├── DailyReport.jsx
│   │   │   ├── ReportList.jsx
│   │   │   └── ReportForm.jsx
│   │   ├── labor/
│   │   │   ├── LaborManagement.jsx
│   │   │   └── WorkerList.jsx
│   │   ├── materials/
│   │   │   ├── MaterialManagement.jsx
│   │   │   └── Inventory.jsx
│   │   └── common/
│   │       ├── Sidebar.jsx
│   │       ├── Header.jsx
│   │       └── Loading.jsx
│   ├── config/
│   │   └── firebase.js
│   ├── hooks/
│   │   ├── useAuth.js
│   │   ├── useFirestore.js
│   │   └── useExcel.js
│   ├── services/
│   │   ├── authService.js
│   │   ├── reportService.js
│   │   └── excelService.js
│   ├── utils/
│   │   ├── exportToExcel.js
│   │   ├── dateUtils.js
│   │   └── validators.js
│   ├── pages/
│   │   ├── HomePage.jsx
│   │   ├── DashboardPage.jsx
│   │   ├── ReportsPage.jsx
│   │   └── SettingsPage.jsx
│   ├── styles/
│   │   └── globals.css
│   ├── App.jsx
│   └── index.js
├── public/
│   └── index.html
├── firebase.json
├── firestore.rules
├── tailwind.config.js
└── package.json
```

## 🔧 1. 초기 설정

### Firebase 설정
```bash
# Firebase CLI 설치
npm install -g firebase-tools

# Firebase 프로젝트 초기화
firebase login
firebase init
```

### 필요한 패키지 설치
```bash
# Firebase
npm install firebase

# Excel 처리
npm install xlsx

# 차트 라이브러리
npm install chart.js react-chartjs-2

# 라우팅
npm install react-router-dom

# Tailwind CSS
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# 유틸리티
npm install date-fns
```

## 🔐 2. Firebase 설정

### `src/config/firebase.js`
```javascript
import { initializeApp } from 'firebase/app';
import { getAuth } from 'firebase/auth';
import { getFirestore } from 'firebase/firestore';
import { getStorage } from 'firebase/storage';

const firebaseConfig = {
  apiKey: process.env.REACT_APP_FIREBASE_API_KEY,
  authDomain: process.env.REACT_APP_FIREBASE_AUTH_DOMAIN,
  projectId: process.env.REACT_APP_FIREBASE_PROJECT_ID,
  storageBucket: process.env.REACT_APP_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: process.env.REACT_APP_FIREBASE_MESSAGING_SENDER_ID,
  appId: process.env.REACT_APP_FIREBASE_APP_ID
};

const app = initializeApp(firebaseConfig);
export const auth = getAuth(app);
export const db = getFirestore(app);
export const storage = getStorage(app);
export default app;
```

### `.env` 파일
```
REACT_APP_FIREBASE_API_KEY=your_api_key
REACT_APP_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your_project_id
REACT_APP_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
REACT_APP_FIREBASE_APP_ID=your_app_id
```

## 🎨 3. Tailwind CSS 설정

### `tailwind.config.js`
```javascript
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
    "./public/index.html"
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#e3f2fd',
          100: '#bbdefb',
          500: '#2196f3',
          600: '#1e88e5',
          700: '#1976d2'
        },
        secondary: {
          50: '#f5f5f5',
          100: '#eeeeee',
          500: '#9e9e9e'
        },
        success: '#4caf50',
        warning: '#ff9800',
        error: '#f44336'
      },
      fontFamily: {
        sans: ['Noto Sans KR', 'sans-serif']
      }
    }
  },
  plugins: []
}
```

### `src/index.css`
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700&display=swap');

body {
  font-family: 'Noto Sans KR', sans-serif;
}

/* 커스텀 컴포넌트 스타일 */
.btn-primary {
  @apply bg-primary-600 text-white px-4 py-2 rounded-lg hover:bg-primary-700 transition-colors;
}

.btn-secondary {
  @apply bg-gray-200 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-300 transition-colors;
}

.card {
  @apply bg-white rounded-lg shadow-md p-6;
}

.input-field {
  @apply border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-primary-500;
}
```

## 📊 4. Firestore 데이터 모델

### 현장 정보 (sites)
```javascript
{
  id: "site_001",
  name: "서울역 복합개발",
  location: "서울시 중구",
  address: "서울시 중구 남대문로 123",
  startDate: "2024-01-01",
  endDate: "2024-12-31",
  managerId: "user_001",
  managerName: "김관리",
  status: "진행중", // 진행중, 완료, 중단
  contractAmount: 10000000000,
  totalArea: 5000,
  description: "상업시설 및 주상복합 건축",
  createdAt: "2024-01-01T00:00:00Z",
  updatedAt: "2024-01-01T00:00:00Z"
}
```

### 일일 작업 보고 (dailyReports)
```javascript
{
  id: "report_2024_01_01",
  siteId: "site_001",
  date: "2024-01-01",
  weather: "맑음",
  temperature: 15,
  humidity: 65,
  workers: [
    {
      id: "worker_001",
      name: "김노동",
      role: "기술자",
      hours: 8,
      wage: 150000,
      tasks: ["철근작업", "콘크리트타설"]
    }
  ],
  equipment: [
    {
      id: "eq_001",
      name: "굴삭기",
      type: "중장비",
      hours: 6,
      fuel: 45,
      operator: "이기사"
    }
  ],
  materials: [
    {
      id: "mat_001",
      name: "시멘트",
      quantity: 50,
      unit: "톤",
      unitPrice: 80000,
      supplier: "삼성시멘트"
    }
  ],
  progress: {
    foundation: 85,
    structure: 45,
    finishing: 10,
    total: 47
  },
  issues: [
    {
      type: "지연",
      description: "날씨로 인한 작업 지연",
      impact: "2시간"
    }
  ],
  images: [
    "image_url_1",
    "image_url_2"
  ],
  createdBy: "user_001",
  createdAt: "2024-01-01T18:00:00Z"
}
```

### 인건비 관리 (laborCosts)
```javascript
{
  id: "labor_001",
  siteId: "site_001",
  workerId: "worker_001",
  workerName: "김노동",
  month: "2024-01",
  dailyRecords: [
    {
      date: "2024-01-01",
      hours: 8,
      wage: 150000,
      overtime: 2,
      overtimePay: 37500
    }
  ],
  totalHours: 176,
  totalWage: 3300000,
  overtimeHours: 20,
  overtimePay: 750000,
  totalPay: 4050000,
  status: "지급완료",
  createdAt: "2024-01-31T23:59:59Z"
}
```

## 🔑 5. 인증 시스템

### `src/components/auth/Login.jsx`
```jsx
import React, { useState } from 'react';
import { signInWithEmailAndPassword } from 'firebase/auth';
import { auth } from '../../config/firebase';
import { useNavigate } from 'react-router-dom';

const Login = () => {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const [loading, setLoading] = useState(false);
  const navigate = useNavigate();

  const handleLogin = async (e) => {
    e.preventDefault();
    setLoading(true);
    setError('');

    try {
      await signInWithEmailAndPassword(auth, email, password);
      navigate('/dashboard');
    } catch (err) {
      setError('로그인 실패: ' + err.message);
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="max-w-md w-full space-y-8">
        <div>
          <h2 className="mt-6 text-center text-3xl font-extrabold text-gray-900">
            스마트 건설 관리 시스템
          </h2>
          <p className="mt-2 text-center text-sm text-gray-600">
            로그인하여 관리 시스템에 접속하세요
          </p>
        </div>
        
        <form className="mt-8 space-y-6" onSubmit={handleLogin}>
          <div className="space-y-4">
            <div>
              <label htmlFor="email" className="block text-sm font-medium text-gray-700">
                이메일 주소
              </label>
              <input
                id="email"
                type="email"
                required
                className="input-field mt-1 block w-full"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                placeholder="이메일을 입력하세요"
              />
            </div>
            
            <div>
              <label htmlFor="password" className="block text-sm font-medium text-gray-700">
                비밀번호
              </label>
              <input
                id="password"
                type="password"
                required
                className="input-field mt-1 block w-full"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                placeholder="비밀번호를 입력하세요"
              />
            </div>
          </div>

          {error && (
            <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded">
              {error}
            </div>
          )}

          <div>
            <button
              type="submit"
              disabled={loading}
              className="btn-primary w-full py-3"
            >
              {loading ? '로그인 중...' : '로그인'}
            </button>
          </div>
        </form>
      </div>
    </div>
  );
};

export default Login;
```

## 📈 6. 대시보드 컴포넌트

### `src/components/dashboard/Dashboard.jsx`
```jsx
import React, { useEffect, useState } from 'react';
import { collection, getDocs, query, where, orderBy, limit } from 'firebase/firestore';
import { db } from '../../config/firebase';
import { Line, Bar, Doughnut } from 'react-chartjs-2';
import { format } from 'date-fns';
import { ko } from 'date-fns/locale';

const Dashboard = () => {
  const [reports, setReports] = useState([]);
  const [sites, setSites] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const fetchData = async () => {
      try {
        // 최근 일일보고 가져오기
        const reportsQuery = query(
          collection(db, 'dailyReports'),
          orderBy('date', 'desc'),
          limit(7)
        );
        const reportsSnapshot = await getDocs(reportsQuery);
        const reportsData = reportsSnapshot.docs.map(doc => ({
          id: doc.id,
          ...doc.data()
        }));
        setReports(reportsData);

        // 현장 정보 가져오기
        const sitesSnapshot = await getDocs(collection(db, 'sites'));
        const sitesData = sitesSnapshot.docs.map(doc => ({
          id: doc.id,
          ...doc.data()
        }));
        setSites(sitesData);
      } catch (error) {
        console.error('데이터 로딩 실패:', error);
      } finally {
        setLoading(false);
      }
    };

    fetchData();
  }, []);

  // 진행률 차트 데이터
  const progressChartData = {
    labels: ['기초공사', '구조공사', '마감공사'],
    datasets: [{
      label: '전체 평균 진행률 (%)',
      data: [75, 45, 20],
      backgroundColor: [
        'rgba(33, 150, 243, 0.8)',
        'rgba(76, 175, 80, 0.8)',
        'rgba(255, 152, 0, 0.8)'
      ],
      borderColor: [
        'rgba(33, 150, 243, 1)',
        'rgba(76, 175, 80, 1)',
        'rgba(255, 152, 0, 1)'
      ],
      borderWidth: 1
    }]
  };

  // 일일 작업자 수 차트 데이터
  const workerChartData = {
    labels: reports.map(r => format(new Date(r.date), 'MM/dd', { locale: ko })),
    datasets: [{
      label: '작업자 수',
      data: reports.map(r => r.workers?.length || 0),
      borderColor: 'rgb(33, 150, 243)',
      backgroundColor: 'rgba(33, 150, 243, 0.1)',
      tension: 0.4
    }]
  };

  if (loading) {
    return (
      <div className="flex items-center justify-center h-64">
        <div className="text-lg text-gray-500">로딩 중...</div>
      </div>
    );
  }

  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold mb-6">대시보드</h1>
      
      {/* 요약 카드 */}
      <div className="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
        <div className="card">
          <div className="flex items-center justify-between">
            <div>
              <p className="text-sm text-gray-600">총 현장 수</p>
              <p className="text-2xl font-bold text-primary-600">{sites.length}</p>
            </div>
            <div className="text-3xl text-primary-500">🏗️</div>
          </div>
        </div>
        
        <div className="card">
          <div className="flex items-center justify-between">
            <div>
              <p className="text-sm text-gray-600">진행 중 현장</p>
              <p className="text-2xl font-bold text-success">
                {sites.filter(s => s.status === '진행중').length}
              </p>
            </div>
            <div className="text-3xl text-success">🚧</div>
          </div>
        </div>
        
        <div className="card">
          <div className="flex items-center justify-between">
            <div>
              <p className="text-sm text-gray-600">오늘 작업자</p>
              <p className="text-2xl font-bold text-warning">
                {reports[0]?.workers?.length || 0}
              </p>
            </div>
            <div className="text-3xl text-warning">👥</div>
          </div>
        </div>
        
        <div className="card">
          <div className="flex items-center justify-between">
            <div>
              <p className="text-sm text-gray-600">평균 진행률</p>
              <p className="text-2xl font-bold text-primary-600">47%</p>
            </div>
            <div className="text-3xl text-primary-500">📊</div>
          </div>
        </div>
      </div>

      {/* 차트 섹션 */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-8">
        <div className="card">
          <h2 className="text-xl font-semibold mb-4">공정 진행률</h2>
          <div className="h-64">
            <Doughnut data={progressChartData} />
          </div>
        </div>
        
        <div className="card">
          <h2 className="text-xl font-semibold mb-4">최근 7일 작업자 현황</h2>
          <div className="h-64">
            <Line data={workerChartData} />
          </div>
        </div>
      </div>

      {/* 현장 목록 */}
      <div className="card">
        <h2 className="text-xl font-semibold mb-4">현장 현황</h2>
        <div className="overflow-x-auto">
          <table className="min-w-full divide-y divide-gray-200">
            <thead className="bg-gray-50">
              <tr>
                <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  현장명
                </th>
                <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  위치
                </th>
                <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  상태
                </th>
                <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  관리자
                </th>
                <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  계약금액
                </th>
              </tr>
            </thead>
            <tbody className="bg-white divide-y divide-gray-200">
              {sites.map(site => (
                <tr key={site.id}>
                  <td className="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                    {site.name}
                  </td>
                  <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    {site.location}
                  </td>
                  <td className="px-6 py-4 whitespace-nowrap">
                    <span className={`inline-flex px-2 py-1 text-xs font-semibold rounded-full ${
                      site.status === '진행중' 
                        ? 'bg-green-100 text-green-800'
                        : site.status === '완료'
                        ? 'bg-blue-100 text-blue-800'
                        : 'bg-gray-100 text-gray-800'
                    }`}>
                      {site.status}
                    </span>
                  </td>
                  <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    {site.managerName}
                  </td>
                  <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    ₩{site.contractAmount?.toLocaleString() || 0}
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  );
};

export default Dashboard;
```

## 📋 7. Excel 내보내기 기능

### `src/utils/exportToExcel.js`
```javascript
import * as XLSX from 'xlsx';
import { format } from 'date-fns';
import { ko } from 'date-fns/locale';

// 일일보고 Excel 내보내기
export const exportDailyReportsToExcel = (reports, siteName) => {
  // 데이터 변환
  const excelData = reports.map(report => ({
    날짜: format(new Date(report.date), 'yyyy-MM-dd'),
    현장: siteName || report.siteName,
    날씨: report.weather,
    온도: `${report.temperature || 0}°C`,
    습도: `${report.humidity || 0}%`,
    작업자수: report.workers?.length || 0,
    장비수: report.equipment?.length || 0,
    재료수: report.materials?.length || 0,
    기초공사진행률: `${report.progress?.foundation || 0}%`,
    구조공사진행률: `${report.progress?.structure || 0}%`,
    마감공사진행률: `${report.progress?.finishing || 0}%`,
    총진행률: `${report.progress?.total || 0}%`,
    작성자: report.createdByName || '미입력',
    작성일: format(new Date(report.createdAt), 'yyyy-MM-dd HH:mm')
  }));

  // 워크시트 생성
  const ws = XLSX.utils.json_to_sheet(excelData);

  // 워크북 생성
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, '일일보고');

  // 파일 다운로드
  XLSX.writeFile(wb, `일일보고_${siteName}_${format(new Date(), 'yyyy-MM-dd')}.xlsx`);
};

// 인건비 명세서 Excel 내보내기
export const exportLaborCostsToExcel = (laborCosts, month, siteName) => {
  const excelData = laborCosts.map(cost => ({
    직원명: cost.workerName,
    근무일수: cost.dailyRecords?.length || 0,
    총근무시간: cost.totalHours || 0,
    기본급: cost.totalWage || 0,
    초과시간: cost.overtimeHours || 0,
    초과수당: cost.overtimePay || 0,
    총지급액: cost.totalPay || 0,
    지급상태: cost.status || '미지급'
  }));

  // 요약 정보 추가
  const summary = {
    근무일수: laborCosts.length,
    총근무시간: laborCosts.reduce((sum, cost) => sum + (cost.totalHours || 0), 0),
    총지급액: laborCosts.reduce((sum, cost) => sum + (cost.totalPay || 0), 0)
  };

  const ws = XLSX.utils.json_to_sheet(excelData);
  
  // 요약 정보 추가
  XLSX.utils.sheet_add_aoa(ws, [
    ['요약 정보'],
    [`대상 기간: ${month}`],
    [`현장명: ${siteName}`],
    [`총 지급액: ₩${summary.총지급액.toLocaleString()}`],
    []
  ], { origin: 'A1' });

  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, '인건비명세서');
  
  XLSX.writeFile(wb, `인건비명세서_${siteName}_${month}.xlsx`);
};

// 자재 관리 Excel 내보내기
export const exportMaterialsToExcel = (materials, siteName) => {
  const excelData = materials.map(material => ({
    자재명: material.name,
    규격: material.specification || '미입력',
    단위: material.unit,
    현재재고: material.currentStock || 0,
    안전재고: material.safetyStock || 0,
    단가: material.unitPrice || 0,
    총가치: (material.currentStock || 0) * (material.unitPrice || 0),
    공급업체: material.supplier || '미입력',
    입고일: material.lastReceived ? format(new Date(material.lastReceived), 'yyyy-MM-dd') : '미입력',
    상태: material.currentStock <= (material.safetyStock || 0) ? '재고부족' : '정상'
  }));

  const ws = XLSX.utils.json_to_sheet(excelData);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, '자재현황');
  
  XLSX.writeFile(wb, `자재현황_${siteName}_${format(new Date(), 'yyyy-MM-dd')}.xlsx`);
};
```

## 🚀 8. Firebase Hosting 배포

### `firebase.json`
```json
{
  "hosting": {
    "public": "build",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ],
    "headers": [
      {
        "source": "**/*.@(js|css)",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "max-age=31536000"
          }
        ]
      }
    ]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  }
}
```

### `firestore.rules`
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 사용자는 자신의 정보만 읽고 쓸 수 있음
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // 현장 정보 - 인증된 사용자만 접근 가능
    match /sites/{siteId} {
      allow read, write: if request.auth != null;
    }
    
    // 일일보고 - 인증된 사용자만 접근 가능
    match /dailyReports/{reportId} {
      allow read, write: if request.auth != null;
    }
    
    // 인건비 정보 - 인증된 사용자만 접근 가능
    match /laborCosts/{costId} {
      allow read, write: if request.auth != null;
    }
    
    // 자재 정보 - 인증된 사용자만 접근 가능
    match /materials/{materialId} {
      allow read, write: if request.auth != null;
    }
  }
}
```

### 배포 스크립트
```bash
#!/bin/bash
# deploy.sh

echo "🚀 스마트 건설 관리 시스템 배포 시작..."

# 1. 의존성 설치
echo "📦 의존성 설치 중..."
npm install

# 2. 빌드
echo "🔨 애플리케이션 빌드 중..."
npm run build

# 3. Firebase 배포
echo "🌐 Firebase에 배포 중..."
firebase deploy --only hosting

echo "✅ 배포 완료!"
echo "🔗 앱 URL: https://your-project.firebaseapp.com"
```

## 📝 9. 개발 순서 체크리스트

### 1단계: 기본 설정
- [ ] Firebase 프로젝트 생성
- [ ] React 프로젝트 초기화
- [ ] Tailwind CSS 설정
- [ ] Firebase SDK 설치 및 설정
- [ ] 기본 라우팅 구조 설정

### 2단계: 인증 시스템
- [ ] 로그인/회원가입 페이지 구현
- [ ] Firebase Auth 연동
- [ ] 인증 상태 관리 (Context API)
- [ ] 라우트 보호 구현

### 3단계: 데이터 모델링
- [ ] Firestore 데이터베이스 구조 설계
- [ ] 컬렉션 및 문서 정의
- [ ] 보안 규칙 설정
- [ ] 인덱스 생성

### 4단계: 핵심 기능 구현
- [ ] 대시보드 페이지
- [ ] 현장 관리 기능
- [ ] 일일 보고 작성 기능
- [ ] 인건비 관리 기능
- [ ] 자재 관리 기능

### 5단계: 데이터 시각화
- [ ] Chart.js 차트 구현
- [ ] 대시보드 통계 구현
- [ ] 실시간 데이터 업데이트

### 6단계: Excel 연동
- [ ] SheetJS 설치 및 설정
- [ ] 일일보고 Excel 내보내기
- [ ] 인건비 명세서 내보내기
- [ ] 자재 현황 내보내기

### 7단계: UI/UX 개선
- [ ] 반응형 디자인 구현
- [ ] 로딩 상태 처리
- [ ] 에러 처리 및 안내
- [ ] 애니메이션 및 트랜지션

### 8단계: 테스트 및 배포
- [ ] 기능 테스트
- [ ] 크로스 브라우징 테스트
- [ ] Firebase Hosting 배포
- [ ] 도메인 연결 (선택사항)

## 🛠️ 10. 유용한 유틸리티 함수

### `src/utils/dateUtils.js`
```javascript
import { format, startOfMonth, endOfMonth, eachDayOfInterval } from 'date-fns';
import { ko } from 'date-fns/locale';

// 날짜 포맷팅
export const formatDate = (date, formatStr = 'yyyy-MM-dd') => {
  return format(new Date(date), formatStr, { locale: ko });
};

// 월의 모든 날짜 가져오기
export const getDaysInMonth = (date) => {
  const start = startOfMonth(date);
  const end = endOfMonth(date);
  return eachDayOfInterval({ start, end });
};

// 근무일 계산 (주말 제외)
export const calculateWorkDays = (startDate, endDate) => {
  const days = eachDayOfInterval({ start: startDate, end: endDate });
  return days.filter(day => {
    const dayOfWeek = day.getDay();
    return dayOfWeek !== 0 && dayOfWeek !== 6; // 일요일(0), 토요일(6) 제외
  });
};
```

### `src/utils/validators.js`
```javascript
// 이메일 유효성 검사
export const validateEmail = (email) => {
  const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return re.test(email);
};

// 비밀번호 유효성 검사 (최소 8자, 대문자, 소문자, 숫자 포함)
export const validatePassword = (password) => {
  const re = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)[a-zA-Z\d@$!%*?&]{8,}$/;
  return re.test(password);
};

// 전화번호 유효성 검사
export const validatePhone = (phone) => {
  const re = /^01([0|1|6|7|8|9])-?([0-9]{3,4})-?([0-9]{4})$/;
  return re.test(phone);
};

// 숫자 유효성 검사
export const validateNumber = (value) => {
  return !isNaN(value) && value !== '';
};
```

이 지침을 따라 개발하시면 완전한 서버리스 스마트 건설 현장 관리 시스템을 구축할 수 있습니다! 🎉
