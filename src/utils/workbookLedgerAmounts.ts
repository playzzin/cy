import { calculateVatAmount, calculateVatInclusiveAmount } from './vatAmounts';

export const calculateWorkbookVatAmount = calculateVatAmount;

export const calculateWorkbookTotalAmount = calculateVatInclusiveAmount;
